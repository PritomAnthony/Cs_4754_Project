Steps to get this to work:

Run "npm install" in command line.
Go into the server.js file and input your SQL username and password into the "user" and "password" fields.
Navigate into the "hotel-management-system" folder.
Run "node server.js" in command line.
Go to "http://localhost:3000/" in browser.